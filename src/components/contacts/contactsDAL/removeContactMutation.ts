import { Contact } from "../contactTypes";
import { mockContact } from "../mocks/mockContact";

const removeContactMutation = (input: String) => {
  return mockContact;
};

export default removeContactMutation;
